import * as React from "react";

export interface IAppProps {}

export default function App(props: IAppProps) {
  return (
    <header className="text-xs w-full p-0 relative right-0 flex justify-between items-center">
      <div className="text-xs p-0 flex items-center flex-col"></div>
    </header>
  );
}
